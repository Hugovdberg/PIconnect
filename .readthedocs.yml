# Required
version: 2

build:
  os: ubuntu-lts-latest
  tools:
    python: "3.12"

formats: all

sphinx:
  builder: html
  configuration: conf.py
  fail_on_warning: true

python:
  install:
    - requirements: requirements_dev.txt
